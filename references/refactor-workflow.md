# Refactor 流程（MVP：仅轻量）

## MVP 允许

- 在已映射边界内：抽函数、局部重命名、去重
- 移动代码时，同一改动内更新明确调用方
- 缺测试时，行为保持型改动前先补**表征测试**

## MVP 不做（推迟 / 问用户）

- 跨模块重设计、换层、依赖倒置运动
- 修 bug 时「顺便把一切变好」
- 没有回归计划的重构

## 步骤

1. Map 边界 + 回归计划（手工亦可）
2. 锁住行为（测试或检查清单）
3. 小步推进：一组 diff 一个意图
4. 重跑回归；证据 ≥ L1
5. 范围膨胀 → 停手，更新 Map，取得确认
